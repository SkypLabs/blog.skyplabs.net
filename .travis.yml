sudo: false

language: ruby

rvm:
  - 2.5.3

script: ./scripts/cibuild

branches:
  only:
    - gh-pages

env:
  global:
    - NOKOGIRI_USE_SYSTEM_LIBRARIES=true # speeds up installation of html-proofer
